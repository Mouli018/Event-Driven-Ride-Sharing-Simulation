apiVersion: apps/v1
kind: Deployment
metadata:
  name: matching-service-deployment
  namespace: ride-share-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: matching-service
  template:
    metadata:
      labels:
        app: matching-service
    spec:
      containers:
      - name: matching-service
        image: ghcr.io/mouli018/matching-service:2fec614c0b3e0ce16c59302f1ec89f4e3efaffbf
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 3
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: ride-share-prod
spec:
  selector:
    app: matching-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
  type: ClusterIP
