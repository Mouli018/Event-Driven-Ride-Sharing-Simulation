apiVersion: apps/v1
kind: Deployment
metadata:
  name: matching-service-deployment
  namespace: ride-share-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: matching-service
  template:
    metadata:
      labels:
        app: matching-service
    spec:
      imagePullSecrets:
      - name: ghcr-login-secret
      containers:
      - name: matching-service
        image: ghcr.io/mouli018/matching-service:444dee1b79da2cb05a34f8de06b3956d62871d78
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: ride-share-prod
spec:
  selector:
    app: matching-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
  type: ClusterIP
